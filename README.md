# orthofinder-summary
Summarizes OrthoFinder orthogroup outputs into concise numerical tables for downstream comparative genomics analyses. The script performs lightweight aggregation of orthogroup gene counts across species using base R only.
